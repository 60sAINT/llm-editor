import React from "react";

const ShareCurrFormatButton = () => {
  const handleClick = () => {};

  return (
    <a
      href="#"
      className="w-40 h-8 leading-8 block !text-dropdown-text text-xs cursor-not-allowed"
      onClick={handleClick}
    >
      保留当前样式
    </a>
  );
};

export default ShareCurrFormatButton;
